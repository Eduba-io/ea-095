# DLA Piper × Eduba landing page

One-page pitch built for DLA Piper at eMerge Americas 2026, Gold Sponsor. Addresses the firm's innovation and AI-and-data-analytics practice through a KPMG UK analog and the Ethics Engine paper.

**Note on the wordmark.** No logo file was available in the brand pack (Cloudflare / Vercel security checkpoint blocked CSS and asset extraction during the research window). In place of an `<img>`, the masthead uses a styled text wordmark ("DLA" serif + "PIPER" tracked sans) in the firm's navy (#0C2C40). Jake should replace with the official SVG once he can capture it from a live session.

Default hosting path: `/for/dla-piper`.

Assets: `index.html`, `styles.css`. No `logo.*` file.
